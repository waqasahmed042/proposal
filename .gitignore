# Python
__pycache__/
*.pyc
*.pyo
*.tmp

# Secrets — never commit credentials
.env

# Client documents & signing data — private, stays local to the server
storage/
signing.db

# Node (leftover lockfiles)
node_modules/
/requirements
